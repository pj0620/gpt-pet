description: "This API endpoint is used to receive a base64 encoded image of what GPTPet is currently seeing"
produces:
  - application/json
responses:
    "200":
      description: Successful response
      schema:
        type: object
        properties:
          image:
            type: string
            description: Base64 encoded image