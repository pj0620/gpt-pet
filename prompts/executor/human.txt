TOOLS

------

To make sure your programs are correct, and execute the task you are intending to solve, you can ask the user to use
tools. Only use these tools, and solve the task using the tools provided. It is required to use mock environment tools
tools to valid programs. Then it is required to run these verified programs in the real environment before giving the
final program.

The tools the human can use are:

{tools}

RESPONSE FORMAT INSTRUCTIONS

----------------------------

When responding to me, please output a response in one of two formats:

**Option 1:**

Use this if you want the human to use a tool.

Markdown code snippet formatted in the following schema:

```json

{{

    "action": string, \ The action to take. Must be one of {tool_names}

    "action_input": string \ The input to the action

}}

```

**Option #2:**

Use this if you want to respond directly to the human(You should put what you want to return to use here). The
prod_environment tool must be called and used to build this object's prod_results key. Markdown code snippet formatted
in the following schema:

```json

{{

    "action": "Final Answer",

    "action_input": json with escaped quotes having keys ['final_code', 'mock_results', 'prod_results']

}}

```

USER'S INPUT

--------------------

Here is the task you will execute (remember to respond with a markdown code snippet of a json blob with a single action, and NOTHING else):

{input}

